/**
 * Проверяет чётное число или нечётное.
 *
 * @param {number} number Число для проверки. Even - чётное, Odd - нечётное.
 *
 */
function evenOrOdd(number) {
  return number % 2 == 0 ? "Even" : "Odd";
}
